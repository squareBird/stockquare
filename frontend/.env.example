# Base URL for the backend API (must start with NEXT_PUBLIC_ to be available in the browser)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Enable in-memory mock data fallback in development when backend is unavailable.
# Accepts: "true" | "false". Never set this to "true" in production builds.
NEXT_PUBLIC_USE_MOCK=false
